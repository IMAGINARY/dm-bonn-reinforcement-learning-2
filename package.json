{
  "name": "dm-bonn-reinforcement-learning-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "degit": "^2.8.4"
  },
  "scripts": {
    "build": "rm -rf ./dist && degit github:IMAGINARY/reinforcement-learning-2#v1.3.2 --force dist && cp -R ./extras/ ./dist && cd ./dist && npm install && npm run build && cd .."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/IMAGINARY/dm-bonn-reinforcement-learning-2.git"
  },
  "private": true
}
